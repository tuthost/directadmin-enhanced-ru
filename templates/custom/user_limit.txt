|*if REASON="inode"||?SUBJECT=Пользователь `USERNAME` использовал `INODE`% из его лимита дескрипторов||*else||?SUBJECT=Пользователь `USERNAME` использовал `BANDWIDTH`% из его трафика и `QUOTA`% из выделенного дискового пространства||*endif|Это автоматическое сообщение уведомляющее что пользователь |USERNAME| использовал следующие ресурсы:
|*if BANDWIDTHLIMITMEG>"0"|Трафик: |BANDWIDTH|%|*endif|
|*if QUOTALIMITMEG>"0"|Дисковое пространство: |QUOTA|%|*endif|
|*if INODELIMIT>"0"|Файловые дескрипторы: |INODE|%|*endif|

|*if BANDWIDTHLIMITMEG>"0"|Ожидается, что при текущем уровне использование трафика хватит на |TIMELEFT| дней, после чего аккаунт будет заблокирован до конца месяца.|*endif|

Пользователь должен связаться с поддержкой, если это необходимо.

|MSG_FOOTER|